// export const BASE_URL = "http://localhost:3000"; // for lacal
export const BASE_URL = ""; // i have configure nginx which is act as reverse proxy for my backend /api/ i have follow this covnetion for writing api /api/v1/..;
